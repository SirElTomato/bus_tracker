name: track_my_travel
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  built_value: ^6.3.1
  built_collection: ^4.1.0
  rxdart: ^0.20.0
  google_maps_flutter: ^0.3.0+2

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.0.0
  built_value_generator: ^6.3.0
  test: ^1.5.3
  mockito: ^4.0.0

flutter:
  uses-material-design: true
